{
  "name": "family-christmas-gifting",
  "version": "1.0.0",
  "description": "A gifting app for families",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "author": "Jordan Mullen",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "dotenv": "^8.1.0",
    "express": "^4.18.2",
    "monk": "^7.0.0",
    "path": "^0.12.7"
  },
  "engines": {
    "node": "12.8.0"
  }
}
